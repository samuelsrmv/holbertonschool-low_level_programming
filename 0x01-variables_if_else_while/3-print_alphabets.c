#include <stdio.h>
/**
 * main - coment
 * Return: value 0
 */
int main(void)
{
char c, u;
for (c = 'a'; c <= 'z'; c++);
{
putchar(c);
}
for (u = 'A'; u <= 'Z'; u++);
{
putchar(u);
}
putchar('\n');
return (0);
}
