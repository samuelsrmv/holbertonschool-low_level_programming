#include "holberton.h"
/**
 * *_memset - pointer
 * @n: pointers
 * @s: pointer
 * @n: pointers
 * @b: pointers
 * Return: value
 */
char *_memset(char *s, char b, unsigned int n)
{
unsigned int c;
for (c = 0; c < n; c++)
;
s[c] = b;
{
return (s);
}
}
